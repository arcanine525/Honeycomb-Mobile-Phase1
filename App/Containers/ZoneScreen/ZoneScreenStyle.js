import {
    StyleSheet
} from 'react-native'
import {Fonts, Metrics} from '../../Themes/'

const styles = StyleSheet.create({
    container: {
        backgroundColor: 'white',
        flex: 1,
    },
    buttonAddNewZoneContainer: {
        position: 'absolute',
        bottom: 15,
        right: 15
    }
})

export default styles